Also look at OneNote notebook

9/30/2018 Using IndexLocal as the html file for debugging
Working in the Layout folder

Starting up debug environment:
In the terminal screen:  npm start
to get a server instance started.  It will pop up the game web page it is connected to.  I found it easiest to close the page and then start a debug session from Code, which will also open the game page, but in debug mode.